---
interface Props {
	href: string
	class?: string
}

const { href, class: className = '' } = Astro.props
---

<a href={href} class={`text-blue-600 hover:text-blue-800 underline ${className}`}>
	<slot />
</a>

---
interface Props {
	src: string;
	alt: string;
	class?: string;
}

const { src, alt, class: className = '' } = Astro.props;
---

<img src={src} alt={alt} class={`rounded-full border-2 ${className}`} style="border-color: var(--border-color);" />
